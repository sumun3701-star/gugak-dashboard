# 국악 대시보드 — PRD v3 (구현 반영본 / As-Built)

| 항목 | 내용 |
|---|---|
| 문서 버전 | v3.0 |
| 작성일 | 2026-09-02 |
| 작성자 | 해맑은 기러기 |
| 상태 | 구현 완료 (개인용 1차 릴리스) |
| 관련 문서 | [Master PRD](./gugak_dashboard_prd_master_1.md) · [PRD v1](./gugak_dashboard_prd_v1.md) · [PRD v2](./gugak_dashboard_prd_v2.md) |
| 코드 | 이 폴더 (`index.html`, `css/`, `js/`, `server.js` / `server.py`) · 사용법은 [README.md](./README.md) |

> 이 문서는 v1·v2 PRD 이후 실제 구현 과정에서 확정·확장된 사항을 정리한 "만들어진 대로(as-built)" PRD다.
> v1·v2에서 정의한 요구사항은 대부분 유지되며, 아래는 그 위에 더해지거나 바뀐 부분을 중심으로 기술한다.

---

## 1. 개요

### 1.1 목적 (유지)

국악계 소식(공연 캘린더, 국악방송 유튜브)과 개인 작업물·할일·아이디어·스크랩을 한 화면에서 확인하고 기록하는 **개인용 단일 사용자 대시보드**.

### 1.2 사용자 (유지)

- 대상: 본인. 타인과 공유하지 않음. 로그인 없음.
- 사용 맥락: 매일 아침 공연·방송 소식 확인, 주간 단위 할일 계획·체크, 틈틈이 작업물·아이디어·스크랩 기록.

### 1.3 구현 형태 (확정)

| 구분 | 내용 |
|---|---|
| 형태 | 정적 웹페이지(HTML/CSS/바닐라 JS) + **선택적 로컬 프록시 서버** |
| 프록시 | `server.js`(Node) 또는 `server.py`(Python). 표준 라이브러리만 사용, 추가 설치 없음 |
| 프록시 역할 | ① KOPIS 오픈API 중계(브라우저 CORS 우회) ② 유튜브 채널 RSS 중계 ③ 스크랩 페이지 메타태그(제목·요약) 추출 |
| 서버 없이 | `index.html`만 열어도 캘린더(샘플·수동), 주간일정, 투두, 작업물, 스크랩, 아이디어 보드 전부 동작. KOPIS 자동수집만 프록시 필요 |
| 배포/실행 | `start.bat` 더블클릭(서버+브라우저) / Windows 작업 스케줄러 `GugakDashboardServer` 로 로그인 시 자동 실행 / 바탕화면 바로가기가 브라우저를 `--app` 창 모드로 실행 |
| 대상 브라우저 | Chromium 계열(웨일·크롬·엣지). `http://localhost:5173` 접속 권장 |

### 1.4 데이터 저장 (확정)

| 저장소 | 대상 |
|---|---|
| **localStorage** (`gugak.*` 접두사) | 설정, 관심 표시, 개인 일정, 수동 공연, 숨긴 공연, 캘린더 필터, 주간 할일/연동 완료상태, 유튜브 캐시/확인기록, 작업물 게시판 메타, 투두, 스크랩, 스크랩 카테고리, 아이디어 메모, KOPIS 캐시 |
| **IndexedDB** (`gugak-files` DB) | 작업물 게시판 첨부파일 실제 바이트 (localStorage 5MB 한계 회피, 수 GB 가능) |

- 새로고침·재접속 후에도 유지된다.
- **다른 기기/브라우저와는 공유되지 않는다.** 설정 › 데이터 › 내보내기(JSON) / 가져오기로 이전한다. 내보내기 JSON에는 첨부파일도 base64로 포함된다.
- 데이터 저장 위치(origin)는 접속 주소별로 분리되므로 **항상 `http://localhost:5173` 로 접속**한다(파일 직접 열기와 섞지 않는다).

---

## 2. 기능 목록 (v3 기준)

| # | 메뉴 | 상태 | 한 줄 설명 |
|---|---|---|---|
| 1 | 홈 — 공연 캘린더 | v1 + 확장 | KOPIS 자동수집 국악 공연 + 개인 일정을 자체 월간 캘린더로 표시 |
| 2 | 홈 — 주간 일정 | v1 + 확장 | 주간 할일 메모·체크 + 캘린더 단방향 연동 |
| 3 | 국악방송 유튜브 | v2 유지 | 국악방송라디오 채널 최신 업로드 피드 |
| 4 | 투두 게시판 | **v3 신규** | 할 일 / 진행 중 2단 칸반 |
| 5 | 작업물 게시판 | v2 → **성격 변경** | 완료된 작업물(링크·파일) 보관함 |
| 6 | 스크랩 | **v3 신규** | 기사·블로그 URL 스크랩 + 네이버 블로그식 카테고리 |
| 7 | 아이디어 보드 | v2 → **형태 변경** | Padlet 스타일 스티키 메모 보드 |
| 8 | 설정 | v1 + 확장 | API 키, 수집 파라미터, 수동 등록, 숨긴 공연 복원, 파일 한도, 데이터 입출력 |

---

## 3. 정보 구조 (IA)

```
대시보드 (상단 탭)
├── 홈
│   ├── 공연 캘린더
│   │   ├── 상단 고정: "챙길 공연·일정" (별표한 날짜 + 개인 일정, D-day)
│   │   ├── 필터: 공연장 / 지역(수도권·전국) / 관심 공연만
│   │   ├── 월간 그리드 (필요한 주 수만 렌더: 보통 5줄)
│   │   └── 날짜 상세 모달 (공연 링크/별표/숨김, 개인 일정 추가·삭제)
│   └── 주간 일정
│       ├── 캘린더 연동 (읽기전용, 완료 체크)
│       └── 직접 추가한 할일 (추가/삭제/체크)
├── 국악방송 유튜브 — 최신 업로드 그리드 (NEW 배지, 모두 확인함)
├── 투두 게시판 — [할 일] [진행 중] 칸반
├── 작업물 게시판 — 완료 작업물 카드 목록 (파일/링크)
├── 스크랩
│   ├── 카테고리 칩 바 (전체 / 미분류 / 사용자 카테고리)
│   ├── 도구: 검색 / 읽음필터 / 태그필터
│   └── 카드 목록 ("전체"일 때 카테고리별 그룹)
├── 아이디어 보드 — 자유 배치 스티키 메모 캔버스
└── 설정
```

---

## 4. 기능 상세

### 4.1 공연 캘린더

#### 데이터 수집

| ID | 요구사항 | 구현 |
|---|---|---|
| CAL-A1 | KOPIS 자동수집 | 공연예술통합전산망(KOPIS) 오픈API `pblprfr` 를 장르코드 `CCCC`(한국음악/국악)로 조회. 조회기간 = 오늘−7일 ~ 오늘+N일(기본 90, 설정 가능). 최대 10페이지 페이지네이션 |
| CAL-A2 | 프록시 경유 | 브라우저 직접 호출은 CORS로 차단되므로 `server.js`/`server.py` 의 `/api/kopis`·`/api/kopis-detail` 로 중계. 프록시 불가 환경(파일 직접 열기 등)에서는 자동수집 생략 |
| CAL-A3 | 캐시·갱신 | 결과를 localStorage(`gugak.kopisCache`)에 저장. TTL 12시간 — 접속 시 캐시가 오래됐으면 자동 재조회, 실패 시 마지막 캐시 표시 |
| CAL-A4 | 지정 vs 발견 구분 | 공연시설명이 `국립국악원·남산국악당·국립극장·김희수아트센터`(설정 가능) 키워드를 포함하면 "지정 공연장", 아니면 "그 외 국악 공연"으로 분류. 색상·라벨로 구분 |
| CAL-A5 | 중복 제거 | `mt20id` 기준 병합. 분류 충돌 시 "지정 공연장" 우선 |
| CAL-A6 | 지역 | 응답의 `area` 로 수도권(서울·경기·인천) 여부 판정. 지역 필터 기본값 "수도권", "전국" 전환 가능. `area` 가 비어 오면 숨기지 않음 |
| CAL-A7 | 키/프록시 없을 때 | 샘플 공연 8건 + 설정에서 수동 등록한 공연 표시. 상태 문구로 "샘플/수동" 명시 |
| CAL-A8 | 수동 등록 | 설정 › 수동 공연 등록 (공연명·공연장·기간·URL·지정여부). 자동수집이 안 되는 예외 보조 수단 |
| CAL-A9 | 예매 URL | 자동수집 항목 클릭 시 KOPIS 상세(`relateurl`)를 지연 조회해 예매처로 이동, 없으면 KOPIS 공연 페이지로 이동 |

#### 캘린더 UI

| ID | 요구사항 | 구현 |
|---|---|---|
| CAL-B1 | 월간 그리드 (자체) | 월요일 시작. **그 달에 실제 필요한 주(행) 수만 렌더** — 대부분 5줄, 짧으면 4줄, 31일이 토·일 시작이면 6줄 |
| CAL-B2 | 칸 크기 | 칸 세로 최소 150px(모바일 104px). 칸 안에 일정 최대 5개 표시, 초과 시 "+N 더보기" |
| CAL-B3 | 이동 | 이전/다음 달, "오늘" 버튼 |
| CAL-B4 | 필터 | 공연장 6종 체크박스(지정 4 + "그 외 국악 공연" + "개인 일정") / 지역 드롭다운 / "관심 공연만" — 상태는 저장됨 |
| CAL-B5 | 날짜 상세 모달 | 날짜 클릭 → 그날의 공연·개인 일정 리스트. 공연: 관심 체크박스 + 링크 이동 + 삭제(숨김) 버튼. 개인 일정: 삭제 + 하단에 추가 폼 |
| CAL-B6 | 개인 일정 | 제목(필수)·**종류**·시간·메모. 종류 = 업무(보라) / 공연 관람(초록) / 개인(주황). 종류별 색 띠로 공연과 함께 구분 표시(칸·상세 모달·상단 고정 목록·범례). 종류 없는 예전 일정은 "개인"으로 취급. 즉시 캘린더 반영, 삭제 즉시 제거 |
| CAL-B7 | 색상 구분 | **초록 = 공연(지정·그 외) + 공연 관람 개인일정** / 보라 = 업무 / 주황 = 개인. 하단 범례. (지정 vs 그 외 구분은 색이 아니라 상세 모달의 라벨로 표시) |

#### 관심 표시(별표) — v3에서 변경

| ID | 요구사항 | 구현 |
|---|---|---|
| CAL-C1 | **날짜 단위 별표** | 관심 표시는 "공연 전체"가 아니라 **"공연 + 날짜"** 단위로 저장(key: `<공연id>\|YYYY-MM-DD`). 여러 날 진행되는 공연도 **체크한 그 날짜 칸에만 ★** 표시 |
| CAL-C2 | 같은 공연 다중 별표 | 같은 공연을 서로 다른 날짜에 각각 별표 가능 |
| CAL-C3 | 칸 내 정렬 | 각 날짜 칸에서 **별표한 공연이 맨 위**, 그다음 개인 일정, 그다음 나머지 공연 (안정 정렬) |
| CAL-C4 | "관심 공연만" 필터 | 별표한 날짜 칸만 남김 (별표 안 한 날은 그 공연을 숨김) |
| CAL-C5 | 마이그레이션 | 예전 형식(공연 id만)의 별표는 앱 실행 시 "공연 첫날 별표"로 1회 자동 변환 |

#### 상단 고정 목록 "챙길 공연·일정" — v3 신규

| ID | 요구사항 | 구현 |
|---|---|---|
| CAL-D1 | 위치 | 캘린더 네비게이션 아래, 필터 위 (캘린더 맨 위) |
| CAL-D2 | 내용 | 별표한 (공연, 날짜) + 캘린더에 추가한 개인 일정 |
| CAL-D3 | 표시 | 날짜 오름차순 칩. 각 칩에 `M/D` + `오늘/내일/D-n` + 제목 + 부제(공연장/시간). 색 구분 동일 |
| CAL-D4 | 동작 | 칩 클릭 → 해당 날짜 상세 모달. 지난 항목 자동 제외. 챙길 게 없으면 줄 자체 숨김. 24개 초과 시 `+N` |

#### 공연 삭제/숨김 — v3에서 변경

| ID | 요구사항 | 구현 |
|---|---|---|
| CAL-E1 | **확인창 없음** | 상세 모달에서 공연 ✕ 클릭 시 확인 팝업 없이 즉시 처리 |
| CAL-E2 | 되돌리기 | 하단 토스트에 "숨김/삭제: {공연명} [되돌리기]" 6초. 되돌리면 그 공연의 별표 날짜까지 복원 |
| CAL-E3 | 자동수집·샘플 | "숨김" 처리 — id를 `gugak.hiddenPerfs` 에 기록해 재수집해도 다시 나타나지 않음 |
| CAL-E4 | 수동 등록 | 완전 삭제 (`gugak.manualPerfs` 에서 제거) |
| CAL-E5 | 복원 | 설정 › 숨긴 공연 목록에서 공연명·공연장·날짜·"N일 전 숨김"과 함께 개별 "복원" / "모두 복원". 목록은 **기본 접힘**(건수만 표시, 펼치기 버튼), 펼쳐도 300px 내에서 스크롤 |
| CAL-E6 | 자동 삭제 | 숨긴 지 **2개월**이 지난 항목은 앱 실행 시 `hiddenPerfs` 에서 자동 제거. 숨긴 시각(`hiddenAt`)이 없는 예전 항목은 실행 시점 기준으로 보정 |

### 4.2 주간 일정

| ID | 요구사항 | 구현 |
|---|---|---|
| WEEK-1 | 주간 뷰 | 월요일 기준 이번 주. 이전/다음 주, "이번 주" 버튼 |
| WEEK-2 | 직접 할일 | 자유 텍스트 추가/삭제, 완료 체크(취소선), 주별 저장 |
| WEEK-3 | 캘린더 연동 (단방향) | 그 주에 속한 **개인 일정** + **별표한 공연 날짜**가 "캘린더 연동" 목록에 읽기전용으로 표시. 각각 완료 체크 가능(상태 저장) |
| WEEK-4 | 연동 규칙 | 별표는 날짜 단위이므로, 별표한 날짜가 그 주에 있을 때만 나타남. 주간에서 직접 추가한 할일은 캘린더에 나타나지 않음 |

### 4.3 국악방송 유튜브 (v2 유지)

| ID | 요구사항 | 구현 |
|---|---|---|
| YT-1 | 채널 | 국악방송라디오 `@GugakFM991` (채널ID `UChGH9Y7DVCJXFlBtNXTf98A`). 업로드 재생목록 `UUhGH9Y7DVCJXFlBtNXTf98A` |
| YT-2 | 수집 경로 | ① 설정에 YouTube Data API v3 키가 있으면 브라우저에서 직접 조회 ② 없으면 프록시(`/api/youtube-rss`)로 채널 RSS ③ 둘 다 없으면 마지막 캐시만 |
| YT-3 | 표시 | 썸네일·제목·상대 업로드 시각 카드 그리드. 클릭 시 새 탭으로 유튜브 이동 |
| YT-4 | 새 영상 | 확인 안 한 영상에 `NEW` 배지 + 탭에 개수 배지. "모두 확인함" 버튼. 캐시 TTL 6시간 |

### 4.4 투두 게시판 (v3 신규)

| ID | 요구사항 | 구현 |
|---|---|---|
| TODO-1 | 2단 칸반 | **할 일 / 진행 중** 두 칼럼. 헤더에 개수 |
| TODO-2 | 카드 | 제목(필수) + 메모 + 태그 + 등록일 |
| TODO-3 | 이동 | 카드 드래그 앤 드롭 / `‹` `›` 버튼으로 두 칼럼 간 이동 |
| TODO-4 | 완료 | `✓ 완료` 버튼 → 카드 제거 + "되돌리기" 토스트. (별도 완료 칼럼 없음 — 완료된 결과물은 작업물 게시판에 등록하는 흐름) |
| TODO-5 | 편집 | 제목·메모·태그 수정, 삭제 |
| TODO-6 | 저장 | `gugak.todos2` |

### 4.5 작업물 게시판 (v2 → 성격 변경)

> v2에서는 할일/진행중/완료 칸반이었으나, v3에서 **완료된 작업물 보관함**으로 분리·단순화. 작업 흐름(할일/진행중)은 투두 게시판으로 이관.

| ID | 요구사항 | 구현 |
|---|---|---|
| BOARD-1 | 링크형 | 노션·구글 시트 등 URL + 제목 + 설명 + 태그 |
| BOARD-2 | 파일형 | 파일 업로드(기본 선택). **IndexedDB 저장**. 클릭 시 다운로드 |
| BOARD-3 | 파일 크기 | 1개 기본 한도 **50MB** (설정에서 1~500MB). 전체 용량은 브라우저·디스크 여유에 따름(보통 수 GB). 설정에 현재 사용량 표시 |
| BOARD-4 | 목록 | 등록일 최신순 카드 그리드. 상태 없음 |
| BOARD-5 | 편집 | 제목 수정, 삭제(파일은 IndexedDB에서도 제거) |
| BOARD-7 | **2단계 카테고리 (네이버 블로그식)** | 왼쪽 트리 사이드바: 전체보기 / 미분류 / 상위 카테고리(▾▸ 접기, 하위 포함 건수) → 하위 카테고리. "+ 상위" 버튼, 각 행 hover 시 ＋(하위 추가, 상위 행만)·▲·▼(같은 레벨 안에서 순서 이동), 이름 더블클릭 → 이름변경(비우면 삭제). 삭제 시 안의 작업물은 미분류로, 하위 카테고리는 상위로 이동. 등록 폼·카드마다 카테고리 select(들여쓰기 트리). "전체보기"·상위 선택 시 카테고리별 그룹 헤딩, 하위 선택 시 평면. 저장: `boardCats`(배열 순서 = 표시 순서), `boardCatOpen` |
| BOARD-6 | 마이그레이션 | 예전 칸반 board 데이터를 앱 실행 시 1회 분리: `완료` → 작업물 게시판, `할일·진행중` → 투두 게시판 카드(링크·첨부 파일명은 메모로 이전). 예전 localStorage 저장 파일은 IndexedDB로 이전 |

### 4.6 스크랩 (v3 신규)

| ID | 요구사항 | 구현 |
|---|---|---|
| SCRAP-1 | 등록 | 기사·블로그 URL + 제목(필수) + 메모/발췌 + 태그 + 카테고리. 출처 도메인 자동 추출 |
| SCRAP-2 | 정보 가져오기 | 프록시(`/api/fetch-meta`) 로 대상 페이지의 `og:title`·`og:description` 을 읽어 제목·메모 자동 채움 (프록시 없으면 수동 입력) |
| SCRAP-3 | 읽음 관리 | 카드별 읽음 체크박스(읽은 글은 흐리게·취소선) |
| SCRAP-4 | 카테고리 (2단계 트리) | 작업물 게시판과 **동일한 2단계 카테고리 트리**(BOARD-7 참조) — 왼쪽 트리 사이드바, 상위→하위, 접기/건수/순서이동(▲▼)/이름변경·삭제, 카드·폼의 카테고리 select. 공용 모듈 `js/cattree.js`(`App.makeCatTree`)로 두 탭이 같은 구현 공유. 저장: `scrapCats`, `scrapCatOpen`. 기존 1단계 카테고리는 자동으로 상위 카테고리로 인식됨 |
| SCRAP-5 | 보기 | "전체보기"·상위 선택 시 카테고리별 소제목 그룹(`상위 › 하위`), 하위 선택 시 평면. 검색·읽음·태그 필터와 조합 |
| SCRAP-6 | 필터 | 텍스트 검색(제목·메모·출처·카테고리) / 읽음·안읽음 / 태그 칩 — 조합 적용 |
| SCRAP-7 | 편집 | 제목·메모·태그 수정, 삭제 |
| SCRAP-8 | 저장 | `gugak.scraps`, `gugak.scrapCats` |

### 4.7 아이디어 보드 (v2 → 형태 변경)

> v2의 "메모 리스트" → v3에서 Padlet 스타일 자유 배치 보드.

| ID | 요구사항 | 구현 |
|---|---|---|
| NOTE-1 | 스티키 메모 | "+ 메모 추가" 로 생성. 인라인 텍스트 편집(자동 높이) |
| NOTE-2 | 자유 배치 | 메모 상단 손잡이(⋮⋮)를 끌어 위치 이동. 좌표 저장. 보드는 점 격자 배경의 스크롤 캔버스 |
| NOTE-3 | 색상 | 5색(노랑·분홍·파랑·초록·보라) 중 선택 |
| NOTE-4 | 삭제 / 반응형 | 메모 삭제. 좁은 화면(≤560px)에서는 세로 목록으로 자동 전환(드래그 숨김) |
| NOTE-5 | 저장 | `gugak.notes` (기존 메모는 위치·색 자동 부여) |

### 4.8 AI 기능 (Gemini · 선택)

설정에 Gemini API 키를 입력하면 활성화. 브라우저에서 `generativelanguage.googleapis.com` 직접 호출(CORS 허용, 키는 `x-goog-api-key` 헤더), 기본 모델 `gemini-2.5-flash`. 키가 없으면 버튼은 보이되 안내 토스트만.

| ID | 요구사항 | 구현 |
|---|---|---|
| AI-1 | 스크랩 "AI 정리" | 스크랩 URL 입력 후 "✨ AI 정리" → 프록시 `/api/fetch-text` 로 본문 평문 추출 → Gemini(JSON 응답)로 **제목·2~3문장 요약·태그 3~5개·카테고리 추천**(현재 카테고리 트리 목록 중 선택). 폼의 제목·메모·태그·카테고리 select 를 채움(비어있는 칸만). 사용자가 확인 후 "스크랩 추가" |
| AI-2 | 프록시 확장 | `/api/fetch-text` — 대상 페이지를 태그 제거·엔티티 해제한 평문(최대 14KB)으로 반환 (리다이렉트 추적) |

### 4.9 설정

| 구분 | 항목 |
|---|---|
| 외부 데이터 연동 | KOPIS 서비스키 / YouTube Data API v3 키 / **Gemini API 키(선택·AI 기능)** / 프록시 주소 / KOPIS 국악 장르코드(기본 CCCC) / 조회 기간(일) / 지정 공연장 키워드 |
| 작업물 게시판 파일 | 업로드 1개 최대 크기(MB) + 현재 사용량·브라우저 허용량 표시 |
| 수동 공연 등록 | 공연명·공연장·기간·URL·지정여부 추가 / 목록 / 삭제 |
| 숨긴 공연 | 캘린더에서 숨긴 자동수집·샘플 공연 목록 / 개별·일괄 복원 |
| 데이터 | 내보내기(JSON, 첨부파일 base64 포함) / 가져오기 / 전체 초기화(IndexedDB 포함) |

---

## 5. 비기능 요구사항 (v3 반영)

| 구분 | 요구사항 |
|---|---|
| 성능 | 캘린더·피드는 캐시 우선 표시(3초 이내). KOPIS 재조회는 접속 시 배경에서 |
| 데이터 보존 | localStorage + IndexedDB. 새로고침·재접속 후 유지. 저장 실패(용량) 시 토스트 안내 |
| 이식성 | 설정 › 내보내기/가져오기 (첨부파일 포함). 기기 간 자동 동기화는 없음 |
| 개인정보 | 로그인 없음. `noindex` 메타. `localhost` 로컬 접속 전제. API 키는 소스에 없고 브라우저 localStorage 에만 저장. 설정 화면의 키 입력칸은 `password` 마스킹 + **👁 표시는 "표시 비밀번호"(PBKDF2-SHA256 해시로만 저장) 입력 시에만** 해제(20초 후 자동 재마스킹). 데이터 내보내기 JSON 은 키 평문 포함 → `.gitignore` 처리 |
| 반응형 | PC·모바일. 라이트/다크 자동. 넓은 콘텐츠(칸반·보드)는 좁은 화면에서 세로 스택 |
| 실행 편의 | `start.bat` 원클릭 / 작업 스케줄러 로그인 시 자동 실행 / 바탕화면 바로가기(`--app` 창, 사과 아이콘) |
| 유지보수 | 기능별 JS 모듈 분리. 공연장·채널·장르코드·파일 한도는 설정에서 조정 |

---

## 6. 아키텍처 / 파일 구조

```
index.html              화면 구조 · 스크립트 로드 순서
css/style.css            스타일 (분홍 테마, 라이트/다크 자동, 반응형)
js/
  util.js                날짜·DOM·토스트(되돌리기 액션) 헬퍼
  store.js               localStorage 래퍼 (용량 예외 처리, 내보내기/가져오기)
  filedb.js              IndexedDB 래퍼 (게시판 첨부파일)
  cattree.js             2단계 카테고리 트리 공용 모듈 (작업물 게시판·스크랩)
  ai.js                  Gemini API 래퍼 (App.ai)
  settings.js            설정 상태 + 설정 화면 + 수동등록 + 숨긴공연 + 데이터 입출력
  kopis.js               KOPIS 수집·XML 파싱·분류·중복제거·12h 캐시·샘플
  youtube.js             유튜브 API/RSS 수집·NEW 추적·6h 캐시
  calendar.js            공연 캘린더 (CAL-*), 상단 고정, 날짜 상세 모달
  weekly.js              주간 일정 (WEEK-*), 캘린더 단방향 연동
  board.js               작업물 게시판 (완료 보관함) + 예전 데이터 분리 마이그레이션
  todos.js               투두 게시판 (할 일 / 진행 중 칸반)
  scraps.js              스크랩 + 2단계 카테고리 (cattree.js 사용)
  notes.js               아이디어 보드 (Padlet 스타일)
  app.js                 탭 라우팅 · 초기화
server.js / server.py    선택적 로컬 프록시 (KOPIS / YouTube RSS / 스크랩 메타)
start.bat                더블클릭 실행 (서버 + 브라우저)
favicon.png / *.ico      앱 · 바로가기 아이콘
README.md                실행법 · API 키 발급 · 기능↔요구사항 매핑
```

### 6.1 프록시 엔드포인트

| 경로 | 대상 | 용도 |
|---|---|---|
| `GET /api/kopis?path=pblprfr&…` | `kopis.or.kr/openApi/restful/…` | 공연 목록 |
| `GET /api/kopis-detail?id=&service=` | KOPIS 공연 상세 | 예매 URL |
| `GET /api/youtube-rss?channel_id=` | `youtube.com/feeds/videos.xml` | 유튜브 RSS |
| `GET /api/fetch-meta?url=` | 임의 페이지 | `og:title`·`og:description` 추출 (리다이렉트 추적, 250KB 상한) |
| `GET /api/fetch-text?url=` | 임의 페이지 | 태그 제거 평문(최대 14KB) — 스크랩 AI 정리용 |

### 6.2 저장 키 (localStorage `gugak.` 접두사)

| 키 | 내용 |
|---|---|
| `settings` | 설정 전체 |
| `kopisCache` | `{ fetchedAt, items[] }` |
| `interested` | `{ "<공연id>\|YYYY-MM-DD": true }` — 날짜 단위 별표 |
| `personalEvents` | `[{ id, title, date, time, memo }]` |
| `manualPerfs` | 수동 등록 공연 |
| `hiddenPerfs` | `{ "<공연id>": { name, venue, date } }` — 숨김 |
| `calFilters` | `{ venues{}, region, onlyInterested }` |
| `weeklyTodos` | `[{ id, text, done, weekKey }]` |
| `weeklySyncDone` | `{ "<weekKey>\|<srcId>": true }` |
| `ytCache` / `ytSeen` | 유튜브 캐시 / 확인한 영상 id |
| `board` | 작업물 게시판 항목(메타). 파일 바이트는 IndexedDB |
| `boardSplitV2` | 예전 board 분리 마이그레이션 완료 플래그 |
| `todos2` | 투두 게시판 카드 |
| `scraps` / `scrapCats` | 스크랩 / 스크랩 카테고리 |
| `notes` | 아이디어 보드 메모 `[{ id, text, color, x, y, createdAt, updatedAt }]` |

IndexedDB: DB `gugak-files`, store `files` → `{ id, blob, meta:{name,type}, savedAt }`

---

## 7. v1·v2 대비 변경 요약

| 항목 | v1·v2 | v3 |
|---|---|---|
| 관심 표시 | 공연 단위(모든 날 별표) | **공연 + 날짜 단위(체크한 날만 별표)** |
| 캘린더 줄 수 | 고정 6줄 | 필요한 만큼(보통 5줄), 칸 세로 150px |
| 공연 삭제 | (없음) | 즉시 숨김/삭제 + 되돌리기 토스트, 설정에서 복원 |
| 상단 고정 목록 | (없음) | 별표한 날짜 + 개인 일정 요약 스트립 |
| 작업물 게시판 | 3단 칸반(할일/진행중/완료) | 완료 작업물 보관함(목록) |
| 투두 게시판 | (없음) | 신규: 할 일 / 진행 중 2단 칸반 |
| 스크랩 | (없음) | 신규: URL 스크랩 + 카테고리 |
| 아이디어 노트 | 메모 리스트 | Padlet 스타일 자유 배치 보드 |
| 첨부파일 저장 | localStorage(2MB) | IndexedDB(기본 50MB, ~GB) |
| 데이터 내보내기 | localStorage만 | 첨부파일 포함 |
| 자동수집 | 방식 미확정 | KOPIS 오픈API + 로컬 프록시 확정, 12h 캐시 |
| 유튜브 수집 | 방식 미확정 | API 키 or RSS(프록시) 확정 |
| 실행 | 미확정 | 로컬 서버 자동 실행 + 바탕화면 앱 바로가기 |

---

## 8. 남은 이슈 / 향후

- **KOPIS 인증키**: 승인된 서비스키가 있어야 자동수집이 동작한다(미승인 시 `SERVICE KEY IS NOT REGISTERED` 반환).
- **남산국악당 커버리지**: KOPIS "국악(CCCC)" 장르 검색에 포함되면 자동 표시. 누락 시 수동 등록으로 보완(서울 열린데이터광장 연동은 미구현).
- **크로스오버 공연**: KOPIS가 "국악"으로 분류하지 않는 공연은 자동수집에서 빠질 수 있음.
- **다중 기기 동기화**: 현재 없음. 필요 시 내보내기/가져오기로 수동 이전.
- **후보 개선**: 공연 포스터 썸네일 표시, iCal(.ics) 내보내기, 주간 미완료 항목 다음 주 이월, PWA(홈 화면 설치).
